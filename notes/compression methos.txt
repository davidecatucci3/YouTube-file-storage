1) CRC + n parity bits (ALL)

100% SURE DATA RECOVER

SLOW 

let's do some calucaltion to see if its feasible:

IF TXT
let's say 1920 x 1080 = 2.073.600
8 bits data, 8 bits CRC, 3 bits flipped
100.000.000 documents stored in total

so space needed to store CRC + n parity bits is 2.073.600 * 12 * 1.000.000.000 = 3000TB = 3PB (NOT FEASIBLE)
so space needed to store CRC + n parity bits is 2.073.600 * 12 * 100.000.000 = 300TB (MEDIUM FEASIBLE)
so space needed to store CRC + n parity bits is 2.073.600 * 12 * 10.000.000 = 30TB (FEASIBLE)
so space needed to store CRC + n parity bits is 2.073.600 * 12 * 1.000.000 = 3TB (FEASIBLE)

IF IMG
let's say 1920 x 1080 x 3 = 6.220.800
24 bits data, 24 bits CRC, 9 bits flipped
100.000.000 documents stored in total

so space needed to store CRC + n parity bits is 6.220.800 * 33 * 1.000.000.000 = 20000TB = 20PB (NOT FEASIBLE)
so space needed to store CRC + n parity bits is 6.220.800 * 33 * 100.000.000 = 2000TB = 2PB (NOT FEASIBLE)
so space needed to store CRC + n parity bits is 6.220.800 * 33 * 10.000.000 = 200TB (MEDIUM FEASIBLE)
so space needed to store CRC + n parity bits is 6.220.800 * 33 * 1.000.000 = 20TB (FEASIBLE)

2) RANGE GROUP (PERFECT ON TXT)

ALMOST 100% SURE DATA RECOVER

FAST

lets's RF (range factor) = 6

so each letter belongs to not one n umber but to 12 so letter A is not 32 but is in range(16, 48)

this means that i have 255 // 12 = 22 possible eocnding but letters, sopecial sign and numner 0-0 is more then 100

3) WHITE/BLACK (PERFECT ON TXT)

ALMOST 100% SURE DATA RECOVER

FAST

let'say a letter A is 65 so 01000001 then each pixel is 0 store(0, 0, 0) (black) 1 store(255, 255, 255) (white) and each 8 pixel is a letter (PIXEL_SIZE=1, 1x1)

if pixel <= (127, 127, 127) is 0 else is 1 w